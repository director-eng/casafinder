# Hero Image

Place the Dominical/Ojochal aerial photo here as:

  `hero-ojochal.jpg`

Once the file is saved, the site will use it automatically.

The `<img>` in `app/(marketing)/page.tsx` references `/images/hero-ojochal.jpg`.
If the file is missing, the Unsplash placeholder is used as a CSS fallback.
